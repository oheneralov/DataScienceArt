{{--
  AdonisJs provides CSRF Protection by default.
  This mean that we need to send a csrf token for every page, PUT or DELETE request.

  ref: http://adonisjs.com/docs/4.0/csrf
  --}}
{{ csrfField() }}

{{-- Title Field --}}
<div class="mb-8">
  @!component('components.label', { text: 'Title', related: 'title' })
  @!component('components.input', { name: 'title', value: old('title', page.title || ''), required: true })
  <div>
    {{ elIf('<small class="text-xs">$self</small>', getErrorFor('title'), hasErrorFor('title')) }}
  </div>
</div>

{{-- Body Field --}}
<div class="mb-8">
  @!component('components.label', { text: 'Body', related: 'editor' })
  @!component('components.textarea', {name: 'body', value: old('body', page.body || '') })
  @!component('components.dropbox', {name: 'type', value: old('type', page.type || '') })
  
  <div>
    {{ elIf('<small class="form-text">$self</small>', getErrorFor('body'), hasErrorFor('body')) }}
  </div>
</div>

<button class="bg-brand hover:bg-brand-dark text-white py-2 px-4 rounded">
  Save
</button>
